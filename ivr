from flask import Flask, Response, request
import plivoxml
PLIVO_SONG="https://s3-eu-west-1.amazonaws.com/mans-answers/answer.xml"
app=Flask(__name__)

@app.route('/master/ivr/', methods=['GET','POST'])
def ivr():
	response = plivoxml.Response()
	params = {
		'action' : "",
		'method' : "POST"
	}
	getDigits = response.addGetDigits(**params)
	getDigits.addSpeak("Enter pin number followed by hash key")
	
	response.addSpeak("Input not received. Thank you.")
	return Response(str(response), mimetype='text/xml')



if __name__ == "__main__":
	app.run(host='0.0.0.0', debug=True)
